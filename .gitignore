# This app holds no credential — the AI lives in LIVE, which holds the key.
# These stay ignored anyway so a stray local file can never become a commit.
.env
.env.*

node_modules/
*.log
.DS_Store

# Local Claude Code preview configuration; per machine, not part of the product.
.claude/
