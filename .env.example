# Copy this file to `.env` and put your real key in it.
# `.env` is gitignored — it must never be committed or pushed.
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# WORKSHOP_MODEL=claude-haiku-4-5
# PORT=3000
