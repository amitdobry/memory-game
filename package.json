{
  "name": "kids-ai-workshop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Memory-match game the kids finish with AI — workshop starter (the 75%)",
  "scripts": {
    "start": "node --env-file-if-exists=.env server/index.js",
    "dev": "node --watch --env-file-if-exists=.env server/index.js",
    "build:avatars": "node scripts/make-avatars.mjs"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.72.0"
  },
  "engines": {
    "node": ">=20"
  }
}
