{
  "name": "kids-ai-workshop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Memory game the kids finish with AI — static site; the AI lives in LIVE",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "build:avatars": "node scripts/make-avatars.mjs",
    "export:contract": "node scripts/export-contract.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
